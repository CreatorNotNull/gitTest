<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.yueqian.com.dao.CardDao"><!-- 用于绑定dao接口 -->
	<select id="getById" parameterType="int" resultMap="cardResultMap">
		SELECT p.`id` AS personId,p.`name` AS personName,c.* 
			FROM person p,card c 
			WHERE c.person_id = p.`id` AND c.id=#{id}
	</select>
  <resultMap type="cn.yueqian.com.dao.entity.Card" id="cardResultMap">
  			<id property="id" column="id"/>
  			<result property="num" column="num"/>
  		<association property="person" javaType="cn.yueqian.com.dao.entity.Person">
  	    	<id property="id" column="personId"/>
  			<result property="name" column="personName"/>
  		</association>
  </resultMap>
  
 </mapper>
